apiVersion: score.dev/v1b1
metadata:
  name: jokes
containers:
  jotd:
    image: .
resources:
  jotd-dns:
    type: dns
  jotd-route:
    type: route
    params:
      path: /
      port: 8080
      host: ${resources.jotd-dns.host}
