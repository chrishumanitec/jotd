id: sample-app
workload:
  externals:
    dns:
      type: dns
  profile: humanitec/default-module
  spec:
    containers:
      main:
        variables: {}
    ingress:
      rules:
        externals.dns:
          http:
            '*':
              port: 8080
              type: default
